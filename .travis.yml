language: python

services:
  - docker

before_install:
    - docker pull gcr.io/panoptes-survey/panoptes-utils
    
script:
    - docker run -it $ci_env gcr.io/panoptes-survey/panoptes-utils python3 setup.py test
