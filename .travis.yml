dist: xenial
sudo: required
language: python
addons:
  apt:
    packages:
      - docker-ce
      - docker-compose
python:
  - "3.6"
services:
  - docker
before_install:
  - PANOPTES_UTILS="${PWD}" INCLUDE_BASE=true scripts/setup-local-environment.sh
install: true
script:
  - scripts/testing/test-software.sh 0
