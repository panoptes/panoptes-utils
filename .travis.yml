dist: xenial
sudo: required
language: python
addons:
  apt:
    packages:
      - docker-ce
      - docker-compose
python:
  - "3.6"
services:
  - docker
before_install:
  - INCLUDE_BASE=true scripts/setup-local-environment.sh
install: true
script:
  - scripts/testing/test-software.sh 0
