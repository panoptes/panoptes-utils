# Telemetry Daemon

This module contains the FastAPI telemetry daemon for Panoptes utilities. It provides endpoint implementations for health checks, run management, event ingestion, and a command-line interface for users.
