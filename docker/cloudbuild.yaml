steps:
# Build
# AMD Build
- name: 'docker:stable'
  id: 'amd64-base'
  args:
  - 'build'
  - '-f=docker/Dockerfile'
  - '--build-arg=python:3.8-slim-buster'
  - '--tag=gcr.io/${PROJECT_ID}/panoptes-utils:latest'
  - '.'
  waitFor: ['-']

# Push
- name: 'docker:stable'
  id: 'push-amd64-base'
  args:
  - 'push'
  - 'gcr.io/${PROJECT_ID}/panoptes-utils:latest'
  waitFor: ['amd64-base']

images:
  - 'gcr.io/${PROJECT_ID}/panoptes-utils:latest'
