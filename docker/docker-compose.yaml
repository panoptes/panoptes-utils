version: '3.8'
services:
  config-server:
    image: gcr.io/panoptes-exp/panoptes-utils:latest
    init: true
    container_name: panoptes-config-server
    privileged: true
    network_mode: host
    restart: on-failure
    command: [ "panoptes-config-server run" ]
